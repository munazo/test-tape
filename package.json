{
  "scripts": {
    "test": "tape-watch -r babel-register tests/**/*.js | tap-spec"
  },
  "devDependencies": {
    "babel-register": "^6.23.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.3",
    "tape-watch": "^2.2.4"
  }
}
